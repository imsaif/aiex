export const guidelines: string[] = [
  "Surface AI actions inside existing UI components, not in separate panels.",
  "Trigger agent capabilities from the object the user is already editing.",
  "Preserve document, thread, or canvas context automatically without user setup.",
  "Show agent output inline, adjacent to the content it references.",
  "Cap onboarding to zero: the agent appears when relevant, not on signup.",
  "Replace modal interruptions with inline suggestions the user can ignore."
];
