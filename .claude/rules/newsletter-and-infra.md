---
paths:
  - "src/app/api/**"
  - "src/app/news/**"
  - "scripts/newsletter/**"
  - "prisma/**"
  - "src/data/newsletters.ts"
  - "vercel.json"
---

# Newsletter, Cron & Infrastructure

## Newsletter & Email Management
- `npm run send-newsletter` — generate a pattern-update HTML blob that admin pastes into a new Beehiiv post
- Newsletter broadcasts via **Beehiiv (manual compose)**: admin clicks Publish in our admin UI → post goes live on /news → admin clicks "Copy HTML" → pastes into a new Beehiiv post → Beehiiv sends to subscribers. Beehiiv free/Launch tier has no Posts API, so delivery is intentionally manual.
- Welcome emails via **Beehiiv Automations** (keyed on `signup_source` custom field, triggered by subscriber sync).
- Transactional emails (audit reports, admin watchdog alerts, cron failure alerts) via **Resend free tier** — ~150 emails/month, well under the 3,000/month cap.
- See [Newsletter Documentation](../../docs/NEWSLETTER.md) for complete setup and usage guide

## Newsletter Subscription System (architecture)
- **Prisma ORM** (Postgres on Neon) for subscriber management (local source of truth, mirrored to Beehiiv)
- **Beehiiv free tier** for subscriber sync + welcome emails (via Automations keyed on `signup_source` custom field) + newsletter delivery (admin composes in Beehiiv dashboard by pasting HTML from our admin UI — Beehiiv Posts API is Enterprise-only)
- **Resend free tier** for transactional emails — audit reports (per-user HTML), admin watchdog alerts, cron failure alerts, "newsletter draft ready" admin pings. ~150 emails/month, well under the 3,000/month cap.
- **No direct transactional sends to subscribers**: every subscriber-facing email is a Beehiiv Automation welcome or a Beehiiv-delivered broadcast. Tokenized PDF downloads happen on-page (no email link).
- **API Routes**: `/api/newsletter/subscribe` (Prisma + Beehiiv sync + `signup_source` custom field), `/api/newsletter/publish` (marks as published + revalidates — admin then copies HTML into Beehiiv), `/api/newsletter/send-update` (returns pattern-update HTML for manual Beehiiv paste), `/api/newsletter/unsubscribe`
- **Soft Delete** active/inactive subscriber management
- **Unsubscribe Tokens** for one-click unsubscribe functionality on our own `/unsubscribe` page; Beehiiv posts use Beehiiv's native unsubscribe footer

## Cron Jobs & Scheduled Tasks

| Issue | Date | Solution |
|-------|------|----------|
| **Newsletter silently drops heavy runs at the 60s cap (weekly + retry-dailies)** | Jul 2026 | Root cause of the 2026-06-29 (weekly) and 2026-07-01 (daily) misses. The route runs generation in `after()` background; on Vercel Hobby the function is capped at 60s **including** the deferred work, and once the response is sent the deferred work can be frozen/killed. **Measured locally: the weekly ≈ 53s** (up to 94s with fresh RSS) — it routinely crosses 60s and is hard-killed before the DB insert, so there's no draft and no `sendFailureAlert` (the catch never runs); the only signal is the watchdog "accepted but nothing appeared" email + a healthchecks.io DOWN. Dailies mostly fit (~30s) but a day that triggers the **selection retry** (a 2nd sequential Sonnet call) can also cross 60s (the 07-01 miss). **cron-job.org cannot rescue this — its request timeout is HARD-CAPPED at 30s** (console-confirmed; a value of 60 is rejected with "maximum timeout is 30 seconds"). That means going **synchronous is NOT viable** while cron-job.org is the trigger: it would disconnect at 30s and show red every run + risk double-fires. **Fix (Jul 2): time-budget the daily selection retry** (`route.ts` ~1908, `genStart`/`retryBudgetMs`) — only retry if ≥15s of the 60s budget remains, and cap its abort to the remainder (reserving ~8s for HTML+insert); otherwise skip and fall through to the existing auto-quiet backstop, which still writes a **visible** draft instead of dying on the cap. The weekly's ~53s is inherent (4096 tokens + 7-day compile) and is left on **watchdog-plus-manual-regen**; a job runner with no time cap (GitHub Actions) is the real fix if weekly misses start costing something. **Architecture note:** there is NO separate weekly cron — the single daily cron (03:10 UTC / 8:40 AM IST) auto-generates the weekly on Mondays via `isMonday` (`route.ts:2108`). Don't "re-enable the weekly cron"; it never existed. **A green "Successful (4.28s)" on cron-job.org means only that `after()` returned a fast 200 — it is NOT evidence generation completed.** |
| **Vercel Hobby cron doesn't execute** | Jan 2026 | Vercel free/hobby tier cron jobs don't reliably trigger. Use **cron-job.org** (free) as external trigger instead. `vercel.json` must NOT have `crons` — removed Mar 29 2026. |
| **cron-job.org timeout kills newsletter generation** | Mar 2026 | Newsletter route fetches RSS + calls Claude API. **Fix (Mar 29):** switched Claude model from Sonnet to Haiku (5-10x faster), reduced RSS timeouts from 5s to 3s. Generation now fits well within 60s. Uses `after()` to respond instantly to cron-job.org while running generation in background. |
| **Duplicate cron runs from dual triggers** | Mar 2026 | **Root cause of "every other day" failures.** Vercel Hobby crons in `vercel.json` fired ~50% of days, racing with cron-job.org and causing silent failures via the `after()` race window. **Fix (Mar 29):** removed `crons` from `vercel.json` entirely — cron-job.org is the SOLE trigger. Also added duplicate re-check before DB insert as safety net. **DO NOT re-add crons to vercel.json.** |
| **Vercel 60s function timeout** | Mar 2026 | Vercel Hobby max function duration is 60s (set in `vercel.json`). Newsletter generation must complete within this. If timeouts recur: (1) check the selection-call per-call timeouts (`claudeTimeoutMs`: 40s daily / 50s weekly; retry 40s) leave margin under 60s, (2) check RSS timeout is 3s not 5s, (3) consider reducing RSS_SOURCES count. |
| **Selection model switched Haiku → Sonnet 4.6** | Jun 2026 | Haiku was a Mar-2026 latency pick, but it routinely ignored the multi-constraint selection rules (max-2-per-company → the 3-Figma incident above, max-1-opinion, product-news floor) — those rules live only in the prompt. **Fix (Jun 25):** switched the selection call (both the main pass and the retry) to `claude-sonnet-4-6`, which follows the rules far better. Measured median ~18s on a representative daily prompt (Haiku was ~4.5s), so per-call timeouts were bumped 25s→40s daily and 45s→50s weekly to keep headroom under the 60s cap. Two sequential calls (main + retry) at 40s each = 80s worst case, but each has its own abort and real latency is ~18s, so total stays well under 60s. **The model is intentionally Sonnet now — do NOT revert to Haiku to "fix" a timeout; bump the timeout or trim the pool instead.** Cost delta is negligible (one call/day). |
| **Newsletter skewed dev-focused; Vercel kept appearing 2-3x per issue** | Apr 2026 | Pool was 19/20 dev/AI sources with only Figma as design publication; flat `+30` AI_PRODUCT_SOURCES baseline gave Vercel and Figma equal priority; Claude's `max 2 per company` rule was bypassed because it operates on its generated `product` field (Vercel customer cases get attributed to "Zo Computer", "GitBook" etc.). **Fix (Apr 20):** added 7 design publications (NN/g, Smashing, UX Collective, A List Apart, UX Planet, TLDR Design, Lenny's Newsletter) + 1 curator (Latent Space); replaced flat baseline with `SOURCE_TIER_BASELINE` map (design-pub +50 → tech-news +0); added conditional infra-keyword penalty (-15 if no design keyword); pre-Claude `MAX_ITEMS_PER_SOURCE = 2` cap on the pool before Claude sees it; `isDesignNativeItem()` runs at publish time and flags `qa.designLightWarning` on `structuredData` for the admin reviewer. Tightened all 3 prompts (daily, weekly, weekly-compilation) to explicitly target designers and drop strained takeaways. **Sources tried but dropped:** Framer (no public RSS at any standard path — `/blog/rss.xml`, `/blog/feed/`, `/blog/atom.xml` all 404), all 4 candidate Reddit subreddits (r/UXDesign, r/userexperience, r/web_design, r/Figma all return 403 from rss-parser even with descriptive UA — Reddit gates on IP reputation, not just UA). Revisit Reddit via an aggregator (Feedly bridge, RSS.app, or static-feed GitHub Action) if community voice is still missing after a few issues. |
| **Same company dominated an issue (3 Figma stories) despite the source cap** | Jun 2026 | The 2026-06-25 daily had 3 of 4 items labeled `product: "Figma"`. The source-level pool cap (`MAX_ITEMS_PER_SOURCE`) worked — it keys on the RSS **feed**, so only 2 came from Figma's feed (the 3rd, "Weave…", was sourced from TechCrunch but Claude labeled it Figma). The prompt's "max 2 per company" rule lives **only in the prompt** and Haiku ignored it — same bypass class as the Apr 2026 Vercel-attribution incident, on the product field instead of the source field. The `qa.selectionRuleViolation` enforcement only checked `no_product_news` and `opinion_present`, so nothing flagged it and it sailed into `pending_review`. **Fix (Jun 25):** added a `company_cap` check in `buildQABlock` that counts by normalized `sel.product` (the subscriber-perceived company, the dimension `sourceCounts` misses) and sets `selectionRuleViolation = company_cap (...)` when any product label appears >2×; branched `buildSelectionRetryAddendum` to instruct a diversified re-pick; exposed `qa.productCounts` for the admin reviewer. `MAX_ITEMS_PER_COMPANY = 2`. Exact-normalized match is intentional (no false-positive quiets); harden with brand-token extraction only if Claude starts splitting labels ("Figma" / "Figma Make") to dodge. |
| **Figma still landed exactly 2 stories every issue (at the company cap, not over it)** | Jun 2026 | Follow-up to the 3-Figma incident above. The Jun 25 `MAX_ITEMS_PER_COMPANY = 2` cap stopped 3+, but Figma kept filling **2 of 4** slots day after day, reading as over-represented. Root cause is structural, not a bug: Figma's `figma.com/blog` is the only clean first-party `design-tool` feed (the other 4 — Mobbin, Loom, Raycast, Arc — are Google-News *search* feeds, noisier), `design-tool` has the top-tier `+35` baseline (parity with `ai-lab`), and `design-tool` had no per-source override so it inherited `MAX_ITEMS_PER_SOURCE = 2`. Two Figma blog posts entered the pool, both scored top, Sonnet picked both — all within the rules. **Fix (Jun 26):** added `'design-tool': 1` to `MAX_ITEMS_PER_SOURCE_BY_TIER` so each design tool contributes at most one item/day (intentionally ignores the weekend +1). The `company_cap` (max 2) stays as the backstop. Keeps Figma's strongest item without the daily double. If design-tool coverage now feels too thin, raise this to 2 again rather than touching the company cap. |
| **`curator` tier was structurally dead — Latent Space silently stripped from every pool** | Jul 2026 | Latent Space was added Apr 2026 as the sole `curator`-tier source (+25 baseline). But it's a Substack — every article is `latent.space/p/<slug>` — and the later-added opinion pre-filter (`route.ts:909`, `sorted.filter(item => !isOpinionUrl(item.link))`) treats any non-allowlisted host with a `/^\/p\/[a-z0-9-]+/` path as a Substack ghosthost and drops it **before scoring**. Since `latent.space` wasn't in `KNOWN_PRODUCT_HOSTS`, 100% of its items were filtered out every run — the curator tier could never contribute, and it was invisible in QA telemetry because the drop happens before `poolSize` is computed (reads as "Latent Space had nothing fresh"). Same class as the Apr/Jun bypass incidents: a later guard silently negated an earlier source-mix decision. Surfaced 2026-07-04 while auditing an opinion-heavy pool. **Fix (Jul 4):** added `'latent.space'` to `KNOWN_PRODUCT_HOSTS` in `isOpinionUrl` — exempts it from the `/p/` ghosthost rule while `*.substack.com` and other custom-domain `/p/` hosts stay filtered. Verified: latent.space kept, `someguy.substack.com/p/…` still dropped. **If you ever want a `/p/` Substack treated as a live first-party source (not opinion), allowlist its host here — don't loosen the `/p/` regex.** |
| **The entire curated practitioner-Substack roster was structurally dead — `*.substack.com` blanket-stripped** | Jul 2026 | Direct follow-up to the Latent Space incident above, but far wider blast radius. `isOpinionUrl` (`route.ts:1621`) had `host.endsWith('.substack.com') → return true`, so **every** `*.substack.com` feed we deliberately subscribed to was stripped before scoring — Jakob Nielsen (`jakobnielsenphd.substack.com`), Julie Zhuo / The Looking Glass (`lg.substack.com`), Emily Campbell / Shape of AI (`shapeofai.substack.com`), Design Systems Collective, Christine Vallaure, Ileana Marcut, Xinran Ma, UX Movement, AI/UX Playground — all `designer-voice`/`design-opinion`/`design-pub` sources added on purpose, all contributing 0 items every run. The `latent.space` fix (Jul 4) only rescued the one curator on a *custom* domain; the shared-host `*.substack.com` sources kept dying, invisibly (drop precedes `poolSize`). Surfaced 2026-07-16 via a `dumpPool` diagnostic while testing whether designer-relevant AI-engineering content was being editorially dropped: it wasn't — the highest-scoring recent AI+design item (Nielsen, "AI Agents Change Workflows", score 67) was being stripped upstream by this filter, NOT by Claude's audience clause. **Fix (Jul 16):** added `SUBSCRIBED_FEED_HOSTS` (derived from `RSS_SOURCES` so it never drifts) and wired `!SUBSCRIBED_FEED_HOSTS.has(host)` into **BOTH** opinion branches — the shared-host `*.substack.com` check AND the custom-domain `/p/` ghosthost check. The `/p/` branch matters: custom-domain subscribed Substacks (One Useful Thing `oneusefulthing.org/p/…`, Proof of Concept `proofofconcept.pub/p/…`, Behind the Craft) never hit the `.substack.com` branch, so patching only that one left them dead — the exact too-narrow miss that made the original latent.space fix insufficient. The `SUBSCRIBED` check on the `/p/` branch subsumes the `latent.space` `KNOWN_PRODUCT_HOSTS` special-case. Also removed `*.substack.com` from the two prompt opinion-exclusion lists (daily prompt + retry addendum) and added a practitioner-voice carve-out; QA `opinionCount` auto-follows since it routes through `isOpinionUrl`. **Prompt-strength note (Jul 16):** a soft "you SHOULD include a practitioner voice" was IGNORED by Sonnet even when the pool held a score-67, dead-on-theme Jakob Nielsen agentic-UX item — same "prompt-only rule gets ignored" class as the 3-Figma / product-news-floor incidents. Escalated to a forceful "REQUIRED WHEN AVAILABLE: you MUST include exactly one practitioner/curator item if any is genuinely relevant to designers, even if it means dropping a fourth general news item," with an explicit escape ("ONLY skip if NONE is genuinely relevant"). That version worked (Nielsen selected, `opinionCount` still 0). It is prompt-level, NOT QA-enforced — if it starts forcing weak items on thin days, add a QA+non-quieting-retry check (mirror the product-news floor, but never auto-quiet on a missing voice). Verified deterministically (12 URL cases): all subscribed substacks (shared + custom domain) + `latent.space` → not opinion; `someguy.substack.com`, `unknownarts.co/p/…`, `uxdesign.cc`, `uxplanet.org`, `lennysnewsletter.com`, `medium.com` → still opinion. **The classic opinion mills stay blocked because they're custom domains hit by the explicit checks before the substack branch. If you add a Substack you do NOT want treated as a live source, it can't be in `RSS_SOURCES`. The prompt edits (Layers 2–3) are unverified until the next real cron run — only the Layer-1 URL logic is deterministically tested.** |
| **Same practitioner (Jakob Nielsen) led the daily issue nearly every day** | Jul 2026 | Direct follow-up to the Jul-16 forced-inclusion escalation above — it worked *too* well. Three factors compounded: (1) the practitioner-voice mandate (`route.ts` `buildPrompt`) REQUIRES exactly one practitioner voice and named **Jakob Nielsen first** as the example, framed as the newsletter's "signature"; (2) `jakobnielsenphd.substack.com` publishes ~daily on AI interfaces, so a genuinely-relevant Nielsen item cleared the pool + 7-day dedup essentially every day, making him the eligible pick every day; (3) **final display order = Claude's raw array order** — `generateHTML` renders items in the order returned with **no code re-sort and no featured slot**, and Claude habitually *leads* with the mandated "signature" voice. Net: Nielsen in slot #1 almost daily. Baselines/scoring were NOT the cause (his `designer-voice` baseline is 22, second-lowest). Note there are two "Nielsen" sources — NN/g (`design-pub`, concrete, fine to lead) and the personal Substack (`designer-voice`, the one leading). **Fix (Jul 23):** a render-time guarantee, not another prompt sentence (every prompt-only selection rule in this file has been ignored by Sonnet — 3-Figma, product-news floor, max-2-per-company, the soft practitioner "should"). Added `VOICE_TIERS = {designer-voice, design-opinion, curator}`, a `HOST_TIER` map (feed host → tier, derived from `RSS_SOURCES` like `SUBSCRIBED_FEED_HOSTS` so it never drifts), `isVoiceItem(item)` (resolves an item's tier from its `sourceUrl` host — safe because voice-tier sources are all direct first-party/Substack feeds whose article host == feed host; Google-News search feeds resolve to a non-matching publisher host but are never voice tiers), and `enforceLeadPosition(items)` which, if item #1 is a voice item AND a non-voice item exists later, promotes the first non-voice item to slot 1 (stable for the rest; no-op on all-voice days — never drops the mandated voice). Applied to `dailyData.items` before `generateHTML` on BOTH the main and retry paths, so email HTML and `/news` structuredData agree. Also reordered the prompt examples so Jakob Nielsen is no longer the first-named practitioner (cheap nudge). Verified with 5 behavioral cases (Nielsen-leads→product promoted; already-concrete→no-op; all-voice→no-op; NN/g-below counts as concrete; single-item→no-op). **Scoped to daily only** (the reported problem); weekly left untouched. If a voice genuinely deserves the lead on a slow news day, the all-voice no-op still allows it. **This is render-level, not QA — if you later want to also cap his daily *presence* (not just his slot), add a recent-author rotation check in selection; don't touch this position guard for that.** |
| **Practitioner/opinion posts badged with the SUBJECT company, not the publisher** | Jul 2026 | Same-session follow-up to the position fix. The story-card source badge (`renderStoryCard`, ~`route.ts:1398`) rendered `item.product` — a **free-text label Claude generates for the story's SUBJECT** (e.g. `"Claude (Anthropic)"` for a post *about* Claude skills) — plus a keyword-matched product icon. So Xinran Ma's *Design with AI* Substack post about Claude skills rendered as **"[Anthropic icon] CLAUDE / ANTHROPIC"**, reading as official Anthropic content when it's a third-party practitioner post. Both `/news` and the email are affected because both render the same stored `content` HTML from `generateHTML` (`/news/[slug]` via `dangerouslySetInnerHTML`). **Fix (Jul 23):** for voice-tier items (designer-voice / design-opinion / curator — the same `VOICE_TIERS` set the position guard uses), badge the **real publication** (e.g. "Design with AI") with **no icon** (a company icon would reintroduce the misattribution). Concrete product-news items keep Claude's subject label + icon — there the subject IS the source, which is what a launch badge should show. **The publisher name + tier are resolved from the POOL, not from Claude's label or from the feed host.** First attempt mapped the article `sourceUrl` host against a `RSS_SOURCES`-derived host map — worked for direct feeds (all Substacks, first-party blogs) but MISSED aggregator-relayed feeds whose article host ≠ feed host (Dive Club: feed `media.rss.com`, articles `rss.com`), leaving those still mis-badged. Robust fix: `enrichItemsWithSource(items, newsItems)` resolves each selected item back to its pool `NewsItem` (exact link, then article host — mirrors `buildQABlock`'s `resolveOriginal`) and injects `sourceName` + `sourceTier` onto the `NewsletterItem` before `enforceLeadPosition`/`generateHTML` on BOTH main and retry paths; `isVoiceItem`/`voicePublisherLabel` prefer the injected fields, falling back to the feed-host map only for the weekly-compilation path (no pool — those items already carry the fields from their daily run). This also hardened the position guard, which had the same feed-host fragility. Verified in a real regen: a `product="Claude (Anthropic)"` designer-voice item rendered as `[no-icon] Design with AI`. **`item.product` is Claude's SUBJECT, never trust it as the source of record — the pool's `source`/`sourceTier` are authoritative.** |
| **Paywall body-length filter silently nuked nearly all AI product news** | Jul 2026 | Surfaced 2026-07-23: issues kept coming out all-practitioner-voice with no AI product updates, and QA logged "pool had 1 product-news item." Root cause: `isPaywalled()` (added Jul 15 for Substack truncation) dropped ANY item whose `content:encoded`/`content` body was under `PAYWALL_MIN_BODY_CHARS = 400`. That heuristic is only valid for Substack (which gates by truncating the body). But **Google News search feeds return a ~60-120 char SNIPPET by design**, and **first-party product blogs publish short RSS SUMMARIES** (measured live: Claude-AI Google-News items 61-116 chars, OpenAI blog 152-172 chars) — the full article is free on the source site. So the length rule was dropping EVERY item from the Google-News-sourced product sources (Cursor, Notion, Perplexity, Claude AI, Windsurf, Mobbin, Loom, Raycast, Arc) AND short-summary first-party AI-lab blogs (OpenAI, likely Google AI / Microsoft AI / Hugging Face) — before scoring, invisibly (drop precedes `poolSize`). Only full-body feeds (UXPin, Dovetail, long Substacks) survived, so the pool collapsed to ~1 product item and Claude had nothing but voices to pick. Same failure class as the curator-dead / substack-blanket-strip incidents: a later guard silently negating an earlier source-mix decision. **Fix (Jul 23):** scoped the body-LENGTH test to Substack-family links only — new `isSubstackGatedHost(link)` (shared `*.substack.com` host + custom-domain `/p/<slug>`, same signal as `isOpinionUrl`); the length drop fires only when that's true. Explicit `PAYWALL_MARKERS` still apply to every source. Verified in a real regen: pool jumped **43-67 → 175 items**, `productNews` **0-1 → 3**, no selection-retry needed, and the issue led with real product news (Claude screen-watching, Dovetail MCP). **Substack paywall protection is intact** (shared + custom-domain still length-checked). **If product news dries up again, check this first: `curl` a Google-News feed and confirm bodies are short — if a NEW length guard is re-added unscoped, it will re-nuke them.** |
| **Same practitioner (Jakob Nielsen) selected in EVERY issue** | Jul 2026 | Follow-up to the position/render fixes above — those stopped him *leading* but he still appeared *daily*. Cause: (1) the Jul-16 prompt mandate "you MUST include exactly one practitioner voice"; (2) `jakobnielsenphd.substack.com` publishes ~daily so he's always an eligible candidate; (3) dedup keys on TITLE not AUTHOR, so a new post clears it every day. User (2026-07-23): "why are we placing him in every generation — readers can just go to his Substack." **Fix (Jul 23), two parts:** (a) **Relaxed the mandate** — the daily prompt + retry addendum went from "MUST include exactly one … this newsletter's signature" to "OPTIONAL, quality-gated: include AT MOST ONE, ONLY if genuinely standout; ZERO is fine on a strong product-news day." (b) **Voice rotation** — new `getRecentlyFeaturedVoices(VOICE_ROTATION_DAYS=7)` reads recent daily `structuredData` (prefers injected `sourceName`/`sourceTier`, falls back to `HOST_TIER`/`HOST_NAME` for old drafts) and `aggregateNews({ rotateVoices: type==='daily' })` SKIPS any `VOICE_TIERS` source featured within the window — so a repeat author can't even be a candidate. Roster is ~25 voice sources, so 7 days rotates plenty while guaranteeing no back-to-back repeats. Verified: run logged "excluding recently-featured voices — AI/UX Playground, Jakob Nielsen" and produced a 0-voice, 4-product-news issue. **If voices vanish for too long, lower VOICE_ROTATION_DAYS; if a weak voice still sneaks in, tighten the prompt's "standout" bar — do NOT re-add a hard mandate.** |
| **Google News redirect links dead-end for readers ("invalid web address")** | Jul 2026 | Direct consequence of the paywall-scoping fix above: re-admitting the Google-News search sources (Cursor, Notion, Perplexity, Claude AI, Windsurf, Mobbin, Loom, Raycast, Arc) meant their `link` is a protobuf-encoded `news.google.com/rss/articles/CBMi...` redirect wrapper. User clicked two (2026-07-23) and both dead-ended on Google's "this page is trying to send you to an invalid web address." First attempt resolved them POST-selection and dropped failures — but that shipped an inconsistent TITLE (Claude wrote the title from 4 items, 2 dropped, title named an absent story). **Fix (Jul 23):** resolve at the POOL level, before Claude selects, via `resolvePoolGoogleNewsLinks` (calls `resolveGoogleNewsUrl` — fetches the article interstitial for its `data-n-a-sg`/`data-n-a-ts`, then POSTs Google's `batchexecute` `Fbv4je`/`garturlreq` to get the real publisher URL). Bounded three ways for the 60s cap: wall-clock BUDGET (12s), COUNT cap (top-12 score-sorted), CONCURRENCY (3). Any Google item not resolved to a clean non-opinion URL — failed, un-attempted, or resolved onto an opinion domain — is DROPPED, so Claude only ever sees/titles working links. Graceful degradation: if the endpoint breaks/rate-limits, Google items fall away and the pool leans on first-party feeds. Verified: "kept 8/8", 4-item issue, all real hosts (androidauthority.com, atlassian.com, dovetail.com, figma.com), zero news.google.com. **Two caveats: (1) the batchexecute endpoint is reverse-engineered — Google can change it anytime; the degradation path is the safety net, not a guarantee. (2) BURST resolution rate-limits — measured 12/12 in isolation with delays but partial failures after a session of ~100 calls; production runs once/day (~8-12 calls) so it's fine, but do NOT loop-regenerate against it. (3) The 12s budget is ADDED before Claude — unverified against the real Vercel 60s cap (every test used lifted budgets); if auto-quiets rise, lower `budgetMs`/`maxResolve`.** |
| **Regenerating today's weekly with new logic when daily items are stale** | Apr 2026 | Weekly normally compiles from the past 7 daily newsletters (`getDailyNewsletterItems(7)`). After source/scoring changes, those daily items still reflect the old pipeline. Use `?forceRSS=true` on a weekly run to bypass the compilation path and pull a fresh RSS pool through the new tiered scoring. Combine with `?force=true` to delete an existing draft for today: `curl -H "Authorization: Bearer $CRON_SECRET" "https://www.aiuxdesign.guide/api/cron/generate-newsletter?type=weekly&force=true&forceRSS=true"`. |
| **Publishing an older draft re-stamps it with TODAY's date** | Aug 2026 | `POST /api/newsletter/publish` (`publish/route.ts:31`) and the GET quick-approve path (`:143`) both hardcode `publishDate: new Date()`. `/news` uses `publishDate` for BOTH `orderBy` and the displayed date (`news/page.tsx:69,79`), `/news/[slug]` uses it for `publishedAt` (`:39`), and `sitemap.ts` selects it for `lastModified`. So publishing a backlog draft through the admin UI moves the issue to today, reorders `/news`, and stacks multiple issues on one date. Surfaced 2026-08-03 while publishing a backlog of 07-30 and 08-02 dailies to feed the Monday weekly. **Important: the stored `publishDate` on a pending/rejected draft is already CORRECT** (set at generation time), and the slug already encodes the right date (`ai-ux-daily-jul-30-...`), so the route is destroying good data, not filling a gap. **Workaround used (not a fix):** publish via a direct Prisma write that updates `status` ONLY and never touches `publishDate`; verify with a before/after print of `publishDate` on every row. `updatedAt` does bump (`@updatedAt`) but is inert, nothing user-facing reads it (sitemap and both news pages select `publishDate`). **Real fix when someone touches this route: only set `publishDate` when it is null, or accept an explicit date param.** Until then, do NOT click Publish in the admin UI on a draft older than today. |
| **Quiet-day drafts are unpublishable by construction (`content: ''`)** | Aug 2026 | The auto-quiet backstop writes the draft with `content: ''` (`route.ts:2432`, also `:2214`/`:2249`) while still storing a COMPLETE `structuredData` (title, summary, items with full attribution, takeaway, qa). So a "Quiet Day — Opinion-Heavy Pool" draft can hold 4-5 genuinely good items and zero HTML. Publishing one puts it in the `/news` list with a link that immediately redirects back to the list (`news/[slug]/page.tsx:101-102` bails when content is empty), and there is nothing to paste into Beehiiv. Surfaced 2026-08-03 recovering a week of rejected dailies: 07-29 and 07-31 both had items but 0 bytes of content, while 07-30 (a normal rejected draft) had 19KB and published fine. **Asymmetry worth knowing: the WEEKLY compile only reads `structuredData.items`, so an empty-content daily still feeds the weekly perfectly once published.** The temptation is therefore to publish it just to feed the weekly and re-reject it after; do not, it puts a broken entry on a live site. **Regenerating the HTML is NOT a one-liner:** `generateHTML` (`route.ts:1767`) is module-private and CANNOT simply be exported, because Next.js only permits config exports (`runtime`/`dynamic`/`maxDuration`) from a `route.ts` and an arbitrary export fails the route typecheck. Recovering a quiet-day draft requires extracting `generateHTML` plus `renderMasthead`/`renderStoryCard`/`renderSectionHeader`/`renderDarkCallout`/`renderAnnouncementBanner`/`renderFooterCTA`/`wrapEmailShell`/`getPatternTitle` into a lib module, which touches the exact rendering core the Jul-23 attribution fixes landed in. Not attempted. **Cheaper future fix: have the quiet-day backstop call `generateHTML` too, so the draft is always recoverable.** |
| **`getDailyNewsletterItems` concatenates with no dedup** | Aug 2026 | `route.ts:1390-1401` is a bare `allItems.push(...data.items)` across every published daily in the window, with no key on URL or headline. Normally harmless because the pool-level URL dedup (`getRecentlyUsedUrls`) stops a story running twice across dailies. It stops being harmless when a REJECTED daily is later published: rejected drafts are excluded from dedup (`status: { in: ['published','pending_review'] }`), so their stories were re-picked by a later daily, and republishing the rejected one puts the same story in the weekly pool twice. Measured 2026-08-03: republishing 07-30 and 07-31 would have double-weighted 4 stories (PayBox, blind-user accessibility, Smashing text-box, Replit design suite), all of which had already run in the published 08-01. **Did NOT materialize this run** (Sonnet picked each of the 7 weekly items once), so it is a review-the-draft risk rather than a bug to fix. **Before publishing a backlog of rejected dailies, diff their `structuredData.items` headlines against the already-published days and expect Claude to over-index on anything doubled.** |
| **The "Quiet Day — Opinion-Heavy Pool" backstop mislabelled every hold, and never once fired on a quiet day** | Aug 2026 | Surfaced 2026-08-04 when the user read the day's held draft as "opinion heavy". It wasn't: stored QA said `opinionCount: 0` and the violation was `no_product_news (pool had 9 product-news items but Claude picked 0)`. **Two separate defects.** (a) **The label was hardcoded.** `route.ts` set the title `'AI UX Daily: Quiet Day — Opinion-Heavy Pool'` and an `-opinion-heavy-day` slug for *all three* violation types, while the `summaryMsg` immediately below already branched correctly. Querying every held draft ever generated: **all 9 (2026-05-09 → 2026-08-04) fired `no_product_news`, zero fired `opinion_present`**, `opinionCount` was 0 on 8 of 9. The label was wrong 9/9 times and cost a debugging session that started from the false premise that the opinion filter was broken. **Fixed:** slug is now violation-neutral (`-held`, still the idempotency key; historical rows keep theirs) and title/summary branch on the actual `selectionRuleViolation`. (b) **The bigger one: `no_product_news` is not a signal about the world.** It only fires when `poolHadProductNews` is true, i.e. the pool DID carry launches and the model skipped them. Across all 9 holds the pools had 5–18 product items. **The backstop has therefore never fired on a genuinely quiet day** — it fires on selection failure and then suppresses the issue *and* misreports why. 08-04's pool held `openai.com/index/continuous-voice-interaction-with-gpt-live` (plus `/circles`, a Figma blog post, a Vercel changelog, a Replit post); a local regen produced a clean 4-item issue **led by that same OpenAI launch**, `productNews=1`, `opinion=0`, no violation, on the first pass. Same class as the 3-Figma / max-2-per-company / soft-practitioner incidents above: a prompt-only rule Sonnet ignores. **Do NOT read a held day as "no news today" — re-run before assuming there was nothing to send.** Two open threads: (1) whether the selection retry even fires in prod is **unknown and unrecorded** — `qa` is only reassigned on retry *success* (`route.ts:2395`), so a retry that ran-and-failed is indistinguishable from one skipped for budget, and Vercel Hobby keeps ~1h of logs. `retryBudgetMs = 60000 − elapsed − 8000` skips below 15s (elapsed > ~37s), and a local run measured **36.0s end-to-end with a 5×-lifted RSS timeout**, so the retry may be structurally unreachable — which would explain 9 holds with zero retry successes ever logged. **Fixed 2026-08-04 (`8251845`):** stored `qa` now carries `retryOutcome` (`not_needed`/`no_addendum`/`skipped_budget`/`succeeded`/`still_violating`/`errored`), `retryBudgetLeftMs`, and `elapsedMs` on both the held and normal paths, so **the next hold answers this definitively — read those three fields first.** The budget guard itself was deliberately left alone: the "retry is unreachable" theory is the weaker one (the 36.0s measurement used a 5× lifted RSS timeout, and prod's 3s timeout likely *lowers* elapsed), and the guard exists because of the real 07-01 silent miss at the 60s cap. Don't loosen it until the telemetry says to. (2) **Held drafts used to be unpublishable by construction** (`content: ''`), even though the daily path had *already rendered* `htmlContent` from that same selection a few lines earlier and simply threw it away — the same waste that blocked recovering the 07-29/07-31 drafts. Also fixed in `8251845`: the held branch stores the rendered HTML, so a held day is now reviewable and publishable rather than lost. It stays `pending_review`, so nothing sends without a human. **Read a held draft before rejecting it — it now contains a real issue.** (3) `?force=true` **hard-deletes** the existing draft (`route.ts` `generateNewsletter`, `delete` not `update`) and its lookup includes `status: 'published'`, so a forced re-run can destroy an issue you already sent as well as the QA block and pool you wanted to investigate — capture them first, and never force-regen a day whose issue is already live. |
| **`productNewsCount` didn't recognise `dev-platform`, so the floor fired on an issue that met it** | Aug 2026 | Surfaced 2026-08-07: the user read the day's draft as "opinion heavy". Stored QA said `opinionCount: 0`, `poolSize: 38`, `productNewsCount: 0`, violation `no_product_news (pool had 12 product-news items but Claude picked 0)` — but the held selection **itself contained the Vercel v0 API launch**. Cause: the counter only credited `ai-lab`/`design-tool` tiers, and Vercel is `dev-platform`, so a genuine launch scored zero and the floor fired on an issue that satisfied it. **Editorial rule set that session: dev-platform counts when it's something a designer would need to know.** **Fix:** one `isProductNewsItem(item)` helper (`route.ts`, defined under the `NewsItem` interface) — ai-lab/design-tool always count; dev-platform counts unless the item is pure infra, reusing the existing conditional infra test (INFRA_KEYWORDS present AND no DESIGN_KEYWORDS_SET hit). **`isDesignNativeItem` was measured and rejected as the gate: it returns FALSE on the v0 API launch**, so it would have reproduced the same false hold. Verified against the live Vercel + GitHub feeds using the route's own `contentSnippet.slice(0, 500)` description: v0 API counts, "Set your own project avatars" and a database-heavy npm-advisory post are excluded. **The helper is called from all THREE sites that encoded the tier list** — the `productNewsCount` increment, the `poolHadProductNews` escape hatch, and the `pool.filter(...)` inside the violation message. That mattered: had only the numerator widened, a day whose pool carried launches solely from `dev-platform` would leave `poolHadProductNews` false, the rule wouldn't fire, and the issue would ship un-guarded — plus the message string would misstate its own cause, which is exactly what sent the 08-04 session sideways. **The gate is deliberately loose** (a GitHub npm-security post passes) because it runs over items Claude ALREADY selected, not over the pool: an off-topic pick is a selection failure with its own guards (`designNativeCount`, company cap), and tightening it re-creates the false hold, the more expensive error. **Not unit-tested — `generateNewsletter`'s helpers are module-private and Next.js rejects arbitrary exports from a `route.ts` (see the quiet-day row above).** The verification above was a scratch replica against live feeds. |

### Manual weekly regeneration (standing runbook)

**When to use:** the weekly is missing (no `type: 'weekly'` row for this Monday — check with the DB snippet below). Production can't reliably generate it — the weekly's Claude compile call routinely exceeds Vercel Hobby's 60s function cap (see the 60s-cap row above), so the Monday auto-weekly silently dies before the DB insert with no alert. We accept manual regen as the standing fallback until the weekly moves to a job runner with no time cap. Confirmed recurring: 2026-06-29, 2026-07-06 and 2026-08-03 all missed (3 of the last 6 Mondays; 07-27 did generate). **On 08-03 there was no `type: 'daily'` row for the Monday either, which is expected, not a second failure: the single daily cron generates the weekly INSTEAD of a daily on Mondays via `isMonday` (`route.ts:2108`).**

**Why local works when production doesn't:** run off-Vercel and there is no 60s function cap. The only remaining limit is the route's own `claudeTimeoutMs` (50s weekly), which the `NEWSLETTER_CLAUDE_TIMEOUT_MS` env var lifts (`route.ts` ~1854). That var is **unset in production**, so prod behavior is unchanged — it exists solely for this runbook. `.env.local` already points at the **prod** Neon DB, so the draft lands in production as `pending_review`, same as a normal cron run.

**Steps** (from repo root; a dev server on :3000 works — no `npm run build`, it clobbers `.next/`):

```bash
# 1. Start dev with the abort lifted (inline, not persisted). Kill any existing :3000 first.
lsof -ti:3000 | xargs kill -9 2>/dev/null
NEWSLETTER_CLAUDE_TIMEOUT_MS=180000 npm run dev   # wait for "Ready"

# 2. Trigger the weekly (uses the LOCAL .env.local CRON_SECRET, not the prod value).
SECRET=$(grep -h '^CRON_SECRET=' .env.local | head -1 | cut -d= -f2- | tr -d '"')
curl -s -H "Authorization: Bearer $SECRET" "http://localhost:3000/api/cron/generate-newsletter?type=weekly"
# → returns immediately; generation runs in after(), takes ~60-90s. Watch the dev log
#   for "compiling from N daily items" then the "INSERT INTO ... NewsletterDraft".
```

Then verify the draft (reuse the DB snippet below with `where: { type: "weekly" }`), and follow the normal publish flow: review at **`/admin/newsletter`** → Publish → Copy HTML → paste into a new Beehiiv post → send.

**Notes:** it takes the **compilation path** (summarizes the week's ~7-13 daily items into one weekly) as long as recent dailies exist — no `forceRSS` needed. If both the full and lite passes abort with "Request was aborted," the env var didn't reach the server (restart dev with it set). No `?force=true` needed unless a partial draft already exists for today.

**cron-job.org Setup:**
- Daily newsletter: `0 3 * * *` (3 AM UTC / 8:30 AM IST) → `https://www.aiuxdesign.guide/api/cron/generate-newsletter`
- Weekly newsletter: `0 2 * * 1` (2 AM UTC / 7:30 AM IST Mon) → `https://www.aiuxdesign.guide/api/cron/generate-newsletter?type=weekly`
- Audit health monitor: `0 * * * *` (hourly) → `https://www.aiuxdesign.guide/api/health/audit` — synthetic monitor for the audit funnel; reuses CRON_SECRET; emails admin via Resend on failure. Healthy checks are silent. See `src/app/api/health/audit/route.ts` for the four checks (env, homepage, analyze route, database). **Cadence: dialed from `*/5` → hourly on 2026-06-16 — real audit volume is ~2/day (see `/admin/audit-samples`), so 5-min checks were 144× the usage rate and kept Neon free-tier compute from auto-suspending (quota risk). Hourly still catches breakage well within the daytime usage window (UTC ~05:00–12:00) and lets Neon sleep ~55 min/hour.**
- Field Web Vitals monitor: `0 5 * * *` (daily, 5 AM UTC) → `https://www.aiuxdesign.guide/api/cron/check-web-vitals` — computes rolling p75 of real-user CWV from the `WebVitalMetric` table (fed by `WebVitalsReporter` → `/api/vitals`); reuses CRON_SECRET; emails admin via Resend ONLY on a "good"-threshold breach (LCP>2.5s/INP>200ms/CLS>0.1), silent otherwise. The field-data layer LHCI (lab) is blind to. Added 2026-06-16.
- Requires `Authorization: Bearer <CRON_SECRET>` header

**"Not much lately" is usually a PUBLISH gap, not a generation failure (Jul 15 2026).** Before assuming missed cron runs, read the prod drafts directly — `curl -H "Authorization: Bearer $ADMIN_APPROVE_SECRET" "https://www.aiuxdesign.guide/api/newsletter/drafts?status=all&limit=6"` (the drafts route accepts `ADMIN_APPROVE_SECRET` as a bearer token for read-only programmatic access — no admin cookie, no generation triggered). Days often generate fine (incl. "Quiet Day" auto-backstop drafts) but sit unpublished in `pending_review`; `/news` only shows *published* issues, so a gap there ≠ no draft. Do NOT probe the cron endpoint without `force` to "check" — a non-force GET with no draft will *start a generation*.

**Source-feed rot check + paywall guard (Jul 15 2026).** Thin pools also come from dead feeds: health-check all `RSS_SOURCES` with rss-parser periodically (fetch each, count items in last 24–48h). Feeds rot silently (Microsoft AI 410, Supabase 404, The Verge path change all found dead Jul 15). The old rss.app/X `designer-voice` bridges are gone (paid + flaky) — practitioner voice now comes from durable first-party RSS + Substack. `isPaywalled()` (route.ts) drops truncated/gated items so readers are never linked to a paywall: **Substack gates by TRUNCATING the RSS body, it does NOT print a reliable marker string** — so the guard keys on `content:encoded` length (< 400 chars = gated/stub), not text. Note `item.content` on Substack is only the *subtitle*; the real body is `content:encoded`.

**Newsletter Troubleshooting Checklist:**

When newsletter doesn't run or emails don't send:

1. **Check cron-job.org has correct CRON_SECRET**
   - Must use the PRODUCTION value from Vercel environment variables
   - NOT the local `.env.local` value (they're different!)

2. **Check cron schedule is correct**
   - Daily: `0 3 * * *` (3 AM UTC = 8:30 AM IST)
   - Weekly: `0 2 * * 1` (2 AM UTC Monday = 7:30 AM IST)

3. **Check database for recent newsletters**
   ```bash
   # Query last 5 newsletters
   DATABASE_URL="..." node -e '
   const { PrismaClient } = require("./src/generated/prisma");
   const prisma = new PrismaClient();
   prisma.newsletterDraft.findMany({ orderBy: { createdAt: "desc" }, take: 5 })
     .then(n => n.forEach(x => console.log(x.createdAt, x.type, x.status, x.title.slice(0,40))))
     .finally(() => prisma.$disconnect());
   '
   ```

4. **Manual trigger (use production CRON_SECRET)**
   ```bash
   curl -H "Authorization: Bearer $CRON_SECRET" \
     "https://www.aiuxdesign.guide/api/cron/generate-newsletter"
   ```

5. **Check Resend dashboard** for transactional email logs (audit reports, admin alerts): https://resend.com/emails

6. **Check Beehiiv dashboard** for subscriber sync + newsletter delivery: https://app.beehiiv.com

7. **Common issues:**
   - 401 Unauthorized → Wrong CRON_SECRET in cron-job.org
   - No newsletter created → Check if "quiet day" (no news) or duplicate prevention blocked it
   - Vercel Runtime Timeout → selection model is intentionally `claude-sonnet-4-6` (Jun 2026; do NOT revert to Haiku). Check the per-call timeouts (`claudeTimeoutMs`: 40s daily / 50s weekly; retry 40s) and that RSS timeout is 3s. Bump the timeout or trim `RSS_SOURCES`, don't downgrade the model.
   - "Every other day" failures → Someone re-added `crons` to `vercel.json`. Remove them — cron-job.org is the sole trigger
   - Transactional emails not sent → Check `RESEND_API_KEY` is set + valid. Resend free tier caps at 100/day + 3,000/month — at normal volumes we use ~5%.
   - Subscriber not synced to Beehiiv → Check BEEHIIV_API_KEY and BEEHIIV_PUBLICATION_ID
   - Admin clicks Publish but newsletter doesn't email subscribers → That's expected. Admin must click "Copy HTML" then paste into a new Beehiiv post and send from Beehiiv. Beehiiv free tier has no Posts API for automation.
   - Welcome email not arriving after signup → Beehiiv publication-level welcome emails must be enabled + Automations keyed on `signup_source` custom field must be configured (values: `direct`, `handbook`, `audit`, `audit-kit`, `news`, `guides`, `agentic-checklist`).

## Deployment & Infrastructure

| Issue | Date | Solution |
|-------|------|----------|
| **`/news` silently falls back to 2 static newsletters** | May 2026 (recurring; observed previously) | `src/app/news/page.tsx` reads from `prisma.newsletterDraft` and merges with static fallback (`src/data/newsletters.ts`, only 2 items left: Dec 17/21 2025). On Prisma/Neon failure the catch returned `[]` and the empty-DB result got cached for the full `revalidate = 60` window — every regeneration overwrote the last-good page with the degraded one, so the page stayed stuck on the 2 static items even though the DB had 50+ rows. **Fix (May 19 2026):** removed the silent `return []` in `getPublishedDrafts()`; the catch now re-throws so Next's ISR retains the last-known-good prerender on transient failures and the error surfaces in Vercel logs. **Detection signals** — if `curl https://www.aiuxdesign.guide/news \| grep -oE 'href="/news/[^"]+"' \| sort -u \| wc -l` returns ≤ 2, this is the failure mode. Confirm by querying `newsletterDraft` directly (use the troubleshooting snippet above) — if DB has rows but page renders ≤ 2, the catch fired. **Don't add silent `return []` catches around DB reads on cached pages** — they turn transient errors into permanently-cached degraded pages. Either throw (so the good cache is preserved) or call `noStore()` from `next/cache` before returning the fallback (so the bad result isn't cached). |

## API & External Services

| Issue | Date | Solution |
|-------|------|----------|
| **Anthropic has no RSS feed** | Dec 2025 | Scrape `anthropic.com/news` page directly instead of using RSS parser |
